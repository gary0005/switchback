---
# Pinned so a run six months from now resolves the same collections. Bump
# deliberately, not by accident.
collections:
  - name: ansible.posix
    version: ">=1.5.4,<3.0.0"
  - name: community.general
    version: ">=9.0.0,<12.0.0"
  - name: community.crypto
    version: ">=2.20.0,<4.0.0"
