---
# Domestic node. It is the only ingress that stays reachable from inside the
# restricted network, so it fronts every chain leaving the country — and it
# never egresses on a user's behalf except on its own test chain.
ansible_host: 203.0.113.10

# Names angie terminates TLS for, and that the certificate covers. The apex is
# not here: it resolves to one node only, because http-01 is answered by
# whichever node the name points at. See inventory/host_vars/v3.
vpn_domains:
  - v0.example.com
